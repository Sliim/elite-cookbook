;; Generated by Chef! Do not edit, your changes will be overriden!
(setq jabber-account-list
  '(<% @accounts.each do |name, config| %>("<%= name %>"
    <% config.each do |k, v| -%>(:<%= k %> . <%= v %>)
    <% end %>)
    <% end %>))
